<%= div_for tweet do %>
    <%= link_to "#{tweet.status.titleize}", tweet %>
    <div class="actions">
      <%= link_to 'edit', edit_tweet_path(tweet) %>
      <%= link_to 'delete', tweet, method: :delete, remote: true %>
    </div>
<% end %>